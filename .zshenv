#export ZDOTDIR=~/.config/zsh-zplug
export ZDOTDIR=~/.config/zsh-sheldon
export ANDROID_SDK="$HOME/Android/Sdk"
. "$HOME/.cargo/env"

# Let user .zshrc initialize completions after custom fpath is configured.
skip_global_compinit=1
